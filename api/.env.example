OPENAI_API_KEY=sk-your-key-here

# Set to 1 to run without API keys (uses canned fixtures)
# MOCK_MODE=1

# whisper.cpp local transcription (avoids faster-whisper/PyAV on macOS)
# TRANSCRIBE_PROVIDER=whispercpp
# WHISPERCPP_MODEL_PATH=api/models/ggml-base.en.bin
# WHISPERCPP_PATH=whisper-cpp  # or whisper-cli if installed via brew
# WHISPERCPP_LANG=en
